name: Lint CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
    paths:
      - 'include/jwt-cpp/**.h'
      - 'tests/**.cpp'
      - '.github/workflows/lint.yml'

jobs:
  cpp:
    runs-on: ubuntu-latest
    container: unibeautify/clang-format:latest
    strategy:
      matrix:
        files: ["include/jwt-cpp/*.h", "tests/**.cpp"]
    steps:
      - uses: actions/checkout@v2
      - run: clang-format --version
      - run: diff -u <(cat ${{ matrix.files }}) <(clang-format-10 ${{ matrix.files }})
